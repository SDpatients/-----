# 5月23日 BUG 汇总 & 待开发功能清单

> **项目：** 供应商协同系统（前端）  
> **技术栈：** Vue 3 + TypeScript + Vite + Element Plus + Pinia  
> **文档日期：** 2026-05-23

---

## 一、已确认的 BUG

### BUG-1: `/orders` 直接访问显示 404

| 属性 | 说明 |
|------|------|
| **严重程度** | 中 |
| **影响范围** | 所有用户直接访问 `/orders` 路径 |
| **复现步骤** | 浏览器访问 `http://localhost:5173/orders` |
| **预期结果** | 正常显示订单页面或重定向到采购订单 |
| **实际结果** | 显示 404 页面（NotFoundView） |

**根因分析：**
路由配置 [router/index.ts](file:///d:/.我的项目/供应商协同/frontend/src/router/index.ts#L34-L102) 中，`/` 根路径重定向到 `/purchasing/dashboard`。订单页面仅定义了以下两条路由：
- `/purchasing/orders` → `OrderListView`（采购方）
- `/supplier/orders` → `SupplierOrderCenterView`（供应商端）

没有定义 `/orders` 裸路由，且无重定向。用户直接输入 `/orders` 会命中 catch-all `/:pathMatch(.*)*`，渲染 `NotFoundView`。

**建议修复：**
- 方案A：在路由中添加 `/orders` 重定向到 `/purchasing/orders`
- 方案B：根据用户角色（internal/supplier）在路由守卫中动态跳转

---

### BUG-2: 供应商审核按钮点击后提示"审核状态不能为空"

| 属性 | 说明 |
|------|------|
| **严重程度** | 高 |
| **影响范围** | `/purchasing/suppliers` 页面的审核功能完全不可用 |
| **复现步骤** | 1. 进入 `/purchasing/suppliers`；2. 点击任意供应商行的「审核」按钮；3. 在弹窗中选择「通过」或「驳回」；4. 点击「确认审核」 |
| **预期结果** | 审核成功或失败提示 |
| **实际结果** | 后端返回错误：「审核状态不能为空」 |

**根因分析：**

1. 前端 `supplierApi.audit()` 调用时发送的请求体为：
   ```typescript
   // supplier.ts:13
   { auditResult: '通过' | '驳回', auditOpinion?: string }
   ```
   
2. 数据库 `supplier_info` 表（[schema.sql:232](file:///d:/.我的项目/供应商协同/database/mysql/schema.sql#L232)）中定义的供应商状态字段为：
   ```sql
   status TINYINT DEFAULT 0 COMMENT '状态(0-待审核,1-合作中,2-暂停合作,3-黑名单)'
   ```

3. 后端审核接口 `/v1/suppliers/{id}/audit` 校验的字段名很可能是 `auditStatus` 或 `status`（数字类型），而非前端发送的 `auditResult`（字符串类型）。前端缺少 `auditStatus` 字段，导致后端校验报错「审核状态不能为空」。

**建议修复：**
将请求体从 `{ auditResult: string }` 改为 `{ auditStatus: number, auditOpinion?: string }`，`auditStatus` 值使用 `auditForm.status`（即 1 通过 / 2 驳回）。

---

### BUG-3: Mock 数据中的供应商状态与筛选器类型不匹配

| 属性 | 说明 |
|------|------|
| **严重程度** | 中 |
| **影响范围** | 供应商管理页面状态筛选功能 |
| **复现场景** | mock 数据模式下，按状态筛选供应商 |

**根因分析：**
- [mockData.ts](file:///d:/.我的项目/供应商协同/frontend/src/api/mockData.ts#L27-L32) 中供应商数据 `status` 字段使用字符串（`'active'`, `'pending'`, `'frozen'`）
- [SupplierListView.vue](file:///d:/.我的项目/供应商协同/frontend/src/views/purchasing/SupplierListView.vue#L119-L124) 中筛选器使用数字（`0: 待审核, 1: 合作中, 2: 暂停合作, 3: 黑名单`）
- [mockApi.ts](file:///d:/.我的项目/供应商协同/frontend/src/api/mockApi.ts#L18) 中 `page()` 函数用 `item.status === query.status` 比较，数字与字符串永远不相等，筛选失效

---

### BUG-4: 供应商资料编辑误用 create 接口

| 属性 | 说明 |
|------|------|
| **严重程度** | 低（取决于后端实现） |
| **文件** | [SupplierProfileView.vue](file:///d:/.我的项目/供应商协同/frontend/src/views/supplier/SupplierProfileView.vue#L54) |

```typescript
const submitEdit = async () => {
  await supplierApi.create({ id: supplierId.value, ...editForm }) // ← 用 create 做更新
}
```

供应商端编辑资料时调用了 `supplierApi.create()` 而非更新接口。虽传了 `id`，但语义错误，且后端可能将 `id` 忽略导致新建一条记录而不是更新。

---

### BUG-5: 报价对比页供应商名称列被隐藏

| 属性 | 说明 |
|------|------|
| **严重程度** | 低 |
| **文件** | [QuoteCompareView.vue](file:///d:/.我的项目/供应商协同/frontend/src/views/purchasing/QuoteCompareView.vue#L87) |

```html
<el-table-column prop="supplierName" label="供应商" min-width="180" v-if="false" />
```

`v-if="false"` 导致供应商名称列永远不会渲染，用户无法看到报价对应的供应商。

---

## 二、待开发 / 未完善的功能

### 2.1 Mock 数据体系未对接（影响全部功能）

**现状：**  
项目在 [mockData.ts](file:///d:/.我的项目/供应商协同/frontend/src/api/mockData.ts) 中定义了完整的 mock 数据（供应商、订单、ASN、质检、对账、待办等），[mockApi.ts](file:///d:/.我的项目/供应商协同/frontend/src/api/mockApi.ts) 中封装了 mock API 调用。但 `main.ts` 中没有任何 mock 拦截器的引入和注册。

**影响：**  
当后端服务（`localhost:8080`）未启动时，所有页面均无法加载数据，显示网络错误。包括但不限于：
- Dashboard 指标
- 供应商/订单/质检/对账 各列表页
- 待办列表
- 附件面板
- 导入导出任务列表

**建议：**  
在 `main.ts` 中引入 mock 拦截器（如 `mockjs` 或 axios interceptor），当后端不可达时自动降级使用本地 mock 数据。

---

### 2.2 RFQ 询价详情页未开发

| 文件 | [RfqListView.vue](file:///d:/.我的项目/供应商协同/frontend/src/views/purchasing/RfqListView.vue#L85-L87) |
|------|------|

```typescript
const showDetail = (row: RfqRecord) => {
  ElMessage.info(`详情页开发中: ${row.rfqNo}`)
}
```

RFQ 列表页点击「详情」仅弹出提示，未实际跳转到详情页。需要开发 RFQ 详情页组件并将路由挂载。

---

### 2.3 业务详情页（BusinessDetailView）明细 Tab 全部占位

| 文件 | [BusinessDetailView.vue](file:///d:/.我的项目/供应商协同/frontend/src/views/purchasing/BusinessDetailView.vue#L132-L144) |
|------|------|

该组件被 5 种详情页复用（供应商、订单、ASN、质量、对账），但「业务明细」Tab 内均为空占位：

```html
<el-empty description="明细数据通过独立接口加载（根据业务模块切换）">
  订单明细接口：GET /api/v1/purchase-order-details?orderId={{ id }}
  送货明细接口：GET /api/v1/delivery-details?noticeId={{ id }}
</el-empty>
```

需要后端对接或使用 mock 数据填充明细表格。

---

### 2.4 细粒度权限校验未启用

| 文件 | [router/index.ts](file:///d:/.我的项目/供应商协同/frontend/src/router/index.ts#L116) |
|------|------|

路由守卫中注释了权限检查逻辑：

```typescript
// 细粒度权限检查暂缓：后端权限命名（如 portal:dashboard）与前端路由 permission meta（如 dashboard:view）不一致，待统一后启用
```

当前只做了 userType（internal/supplier）级别的路由隔离，未进行 per-route 的细粒度权限过滤。需前后端统一权限命名规范后启用。

---

### 2.5 供应商选择器（SupplierSelector）未在多个创建弹窗中使用

以下弹窗直接要求用户**手动输入供应商ID数字**，缺乏 SupplierSelector 组件：

| 页面 | 弹窗 | 
|------|------|
| [VmiForecastView.vue](file:///d:/.我的项目/供应商协同/frontend/src/views/purchasing/VmiForecastView.vue#L43) | VMI库存同步 |
| [PurchasingFinanceView.vue](file:///d:/.我的项目/供应商协同/frontend/src/views/purchasing/PurchasingFinanceView.vue#L50) | 创建发票 |
| [PurchasingFinanceView.vue](file:///d:/.我的项目/供应商协同/frontend/src/views/purchasing/PurchasingFinanceView.vue#L154) | 创建付款 |
| [PurchasingFinanceView.vue](file:///d:/.我的项目/供应商协同/frontend/src/views/purchasing/PurchasingFinanceView.vue#L216) | 创建扣款 |
| [PurchasingFinanceView.vue](file:///d:/.我的项目/供应商协同/frontend/src/views/purchasing/PurchasingFinanceView.vue#L304) | 绩效评估 |

用户体验差，容易输错 ID。

---

### 2.6 缺少导出 / 打印功能对接

项目在 [ImportExportPanel.vue](file:///d:/.我的项目/供应商协同/frontend/src/components/business/ImportExportPanel.vue) 和 [ExportDialog.vue](file:///d:/.我的项目/供应商协同/frontend/src/components/business/ExportDialog.vue) 中定义了导入导出组件，并在详情页的「导入导出」Tab 中引用。但：
- 列表页（供应商/订单/质检/对账等）均缺少「导出」按钮
- 未对接 `ExportDialog` 的实际导出逻辑（需选择导出格式、字段映射等）

---

### 2.7 消息通知中心内容占位

[MessageCenterView.vue](file:///d:/.我的项目/供应商协同/frontend/src/views/MessageCenterView.vue) 作为独立路由 `/messages` 的消息中心页面，目前仅为占位页面，未对接通知列表、已读/未读筛选、批量操作等核心功能。

---

### 2.8 供应商资质证书管理功能未开发

数据库中有 `supplier_qualification` 表，类型系统中有 `SupplierQualificationItem` 和 `Certificate` 类型，但前端：
- [SupplierProfileView.vue](file:///d:/.我的项目/供应商协同/frontend/src/views/supplier/SupplierProfileView.vue) 资料中心缺少资质证书列表
- 没有资质证书的增删改查 UI 和 API 调用

---

### 2.9 采购方财务中心与供应商财务中心的差异未明确

- [PurchasingFinanceView.vue](file:///d:/.我的项目/供应商协同/frontend/src/views/purchasing/PurchasingFinanceView.vue) 包含发票、付款、扣款、供应商绩效四个 Tab
- [SupplierSettlementCenterView.vue](file:///d:/.我的项目/供应商协同/frontend/src/views/supplier/SupplierSettlementCenterView.vue) 包含对账、发票、扣款、付款四个 Tab

两端的发票/扣款/付款功能大量代码重复，且数据源、权限范围是否应该区分（采购方看全部，供应商仅看自己）未明确。

---

### 2.10 全局搜索功能缺失

整个系统没有全局搜索入口，用户无法跨模块搜索供应商、订单、ASN 等内容。

---

## 三、接口与后端对接状态

### 3.1 当前全部依赖真实后端

- 前端通过 Vite proxy 将 `/api` 代理到 `http://localhost:8080`
- 所有 API 调用均使用 `request`（axios 实例），baseURL 为 `/api`
- 没有 mock 拦截器机制，后端不启动则所有功能均不可用

### 3.2 已定义但未验证的 API 模块（已验证）

> **验证日期：** 2026-05-25  
> **验证方法：** 逐文件对照前端 API 模块与后端 Controller，核对 HTTP 方法、URL 路径、参数

| 模块 | 文件 | 验证结果 |
|------|------|----------|
| 库存（VMI/预测） | [inventory.ts](file:///d:/.我的项目/供应商协同/frontend/src/api/inventory.ts) | **通过** - 所有端点与后端匹配 |
| 集成网关 | [integration.ts](file:///d:/.我的项目/供应商协同/frontend/src/api/integration.ts) | **通过** - 所有端点与后端匹配 |
| 系统配置 | [config.ts](file:///d:/.我的项目/供应商协同/frontend/src/api/config.ts) | **通过** - 所有端点与后端匹配 |
| 财务扩展 | [finance.ts](file:///d:/.我的项目/供应商协同/frontend/src/api/finance.ts) | **2 处致命错误** (见 BUG-6, BUG-7) |
| 质量扩展 | [qualityExtra.ts](file:///d:/.我的项目/供应商协同/frontend/src/api/qualityExtra.ts) | **4 处致命错误** (见 BUG-8~11) |
| 寻源 | [sourcing.ts](file:///d:/.我的项目/供应商协同/frontend/src/api/sourcing.ts) | **1 处致命错误** (见 BUG-12) |
| 附件 | [attachment.ts](file:///d:/.我的项目/供应商协同/frontend/src/api/attachment.ts) | **通过** - 所有端点与后端匹配 |
| 导入导出 | [importExport.ts](file:///d:/.我的项目/供应商协同/frontend/src/api/importExport.ts) | **1 处问题** (见 BUG-13) |
| 操作日志 | [operationLog.ts](file:///d:/.我的项目/供应商协同/frontend/src/api/operationLog.ts) | **通过** - 端点匹配，但有死代码 (见 BUG-14) |

---

### 3.3 API 验证发现的 BUG

#### BUG-6: 发票作废接口路径错误

| 属性 | 说明 |
|------|------|
| **严重程度** | **高** |
| **文件** | [finance.ts](file:///d:/.我的项目/供应商协同/frontend/src/api/finance.ts#L20) |
| **前端调用** | `POST /v1/invoices/{id}/cancel` |
| **后端定义** | `POST /v1/invoices/{id}/void` ([InvoiceController.java](file:///d:/.我的项目/供应商协同/backend/src/main/java/com/supplier/settlement/controller/InvoiceController.java#L83)) |
| **影响** | 发票作废功能完全不可用，返回 404 |

#### BUG-7: 供应商绩效 API 基础路径错误

| 属性 | 说明 |
|------|------|
| **严重程度** | **高** |
| **文件** | [finance.ts](file:///d:/.我的项目/供应商协同/frontend/src/api/finance.ts#L55-L64) |
| **前端调用** | `/v1/performances` |
| **后端定义** | `/v1/supplier-performances` ([SupplierPerformanceController.java](file:///d:/.我的项目/供应商协同/backend/src/main/java/com/supplier/settlement/controller/SupplierPerformanceController.java#L29)) |
| **影响** | 供应商绩效的所有 CRUD 操作（分页/详情/新增/更新/删除）均不可用，全返回 404 |

#### BUG-8: 8D 整改报告 API 基础路径错误

| 属性 | 说明 |
|------|------|
| **严重程度** | **高** |
| **文件** | [qualityExtra.ts](file:///d:/.我的项目/供应商协同/frontend/src/api/qualityExtra.ts#L24-L41) |
| **前端调用** | `/v1/eight-d` |
| **后端定义** | `/v1/eight-d-reports` ([EightDReportController.java](file:///d:/.我的项目/供应商协同/backend/src/main/java/com/supplier/quality/controller/EightDReportController.java#L27)) |
| **影响** | 8D 报告的所有操作（分页/详情/新增/更新/提交/审核/退回/关闭）均不可用，全返回 404 |

#### BUG-9: 8D 审核操作路径名错误

| 属性 | 说明 |
|------|------|
| **严重程度** | **高** |
| **文件** | [qualityExtra.ts](file:///d:/.我的项目/供应商协同/frontend/src/api/qualityExtra.ts#L36) |
| **前端调用** | `POST /v1/eight-d/{id}/review` |
| **后端定义** | `POST /v1/eight-d-reports/{id}/audit` |
| **说明** | 即使修复基础路径，`review` 也需改为 `audit` |

#### BUG-10: 8D 退回操作路径名错误

| 属性 | 说明 |
|------|------|
| **严重程度** | **高** |
| **文件** | [qualityExtra.ts](file:///d:/.我的项目/供应商协同/frontend/src/api/qualityExtra.ts#L38) |
| **前端调用** | `POST /v1/eight-d/{id}/return` |
| **后端定义** | `POST /v1/eight-d-reports/{id}/reject` |
| **说明** | 即使修复基础路径，`return` 也需改为 `reject` |

#### BUG-11: NCR 质量异常 API 基础路径错误

| 属性 | 说明 |
|------|------|
| **严重程度** | **高** |
| **文件** | [qualityExtra.ts](file:///d:/.我的项目/供应商协同/frontend/src/api/qualityExtra.ts#L5-L22) |
| **前端调用** | `/v1/ncr` (分页/详情/新增/提交/处理/验证/关闭) |
| **后端定义** | `/v1/nonconformance-reports` ([NonconformanceReportController.java](file:///d:/.我的项目/供应商协同/backend/src/main/java/com/supplier/quality/controller/NonconformanceReportController.java#L27)) |
| **影响** | NCR 所有功能在前后端联调时全部返回 404 |

#### BUG-12: 报价采纳操作路径名错误

| 属性 | 说明 |
|------|------|
| **严重程度** | **高** |
| **文件** | [sourcing.ts](file:///d:/.我的项目/供应商协同/frontend/src/api/sourcing.ts#L52) |
| **前端调用** | `POST /v1/quotes/{id}/adopt` |
| **后端定义** | `POST /v1/quotes/{id}/accept` ([QuoteController.java](file:///d:/.我的项目/供应商协同/backend/src/main/java/com/supplier/sourcing/controller/QuoteController.java#L72)) |
| **影响** | 采购方采纳报价功能不可用，返回 404 |

#### BUG-13: 导入模板列表为硬编码假数据

| 属性 | 说明 |
|------|------|
| **严重程度** | **中** |
| **文件** | [importExport.ts](file:///d:/.我的项目/供应商协同/frontend/src/api/importExport.ts#L17-L20) |
| **问题** | `templates()` 方法未调用后端 API，返回硬编码的静态数据 |
| **影响** | 模板列表永远只有「订单」和「ASN」两项，无法动态获取后端注册的模板 |

#### BUG-14: operationLog.ts 存在未使用的死代码

| 属性 | 说明 |
|------|------|
| **严重程度** | **低** |
| **文件** | [operationLog.ts](file:///d:/.我的项目/供应商协同/frontend/src/api/operationLog.ts#L5-L9) |
| **问题** | 文件顶部定义了 `logs` 数组（3 条硬编码操作日志），但 `operationLogApi.list()` 实际调用了后端 API `/v1/audit-logs`，该数组从未被使用 |

---

## 四、总结

| 类别 | 数量 | 关键项 |
|------|------|--------|
| 已确认 BUG (原有) | 5 | `/orders` 404、审核状态为空、mock 状态匹配、编辑用 create 接口、供应商列隐藏 |
| API 验证发现的 BUG | **9** | 见 BUG-6 ~ BUG-14 |
| 功能未完善 | 10 | Mock 未对接、RFQ详情页、业务明细Tab、权限校验、选择器缺失等 |
| API 通过验证 | 5 | 库存、集成、配置、附件、操作日志 |

**优先建议（更新）：**
1. **紧急修复 API 路径错误**：BUG-7（绩效）、BUG-8/9/10（8D）、BUG-12（报价采纳）均为一字之差导致 404
2. **后端补齐 NCR Controller**（BUG-11），前端 NCR API 已完整定义，阻塞质量模块联调
3. 接入 mock 数据体系，使前端可独立运行开发
4. 修复 BUG-1（/orders 404）和 BUG-2（审核报错）
5. 统一 SupplierSelector 组件到所有需要供应商输入的弹窗
6. 修复 finance.ts 发票取消路径（BUG-6）