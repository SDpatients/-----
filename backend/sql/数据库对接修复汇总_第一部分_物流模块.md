# 后端与数据库对接修复汇总 — 第一部分：物流模块

> 对照基准：`supplier_collaboration.sql`（Navicat 导出的实际数据库结构）
> 分析日期：2026-05-29

---

## 一、delivery_detail（送货明细表）

| 状态 | 字段 | 说明 |
|------|------|------|
| ✅ | noticeId → notice_id | 匹配 |
| ✅ | orderDetailId → order_detail_id | 匹配 |
| ✅ | materialCode → material_code | 匹配 |
| ✅ | materialName → material_name | 匹配 |
| ✅ | materialSpec → material_spec | 匹配 |
| ✅ | unit | 匹配 |
| ✅ | planQty → plan_qty | 匹配 |
| ✅ | actualQty → actual_qty | 匹配 |
| ✅ | receivedQty → received_qty | 匹配 |
| ✅ | qualifiedQty → qualified_qty | 匹配 |
| ✅ | batchNo → batch_no | 匹配 |
| ✅ | productionDate → production_date | 匹配 |
| 🔴 | **expiryDate** → ~~expiry_date~~ | **Java 有此字段，DB 无此列（已建 V1.2 迁移修复）** |
| 🟡 | ~~boxCount~~ ← box_count | DB 有此列，Java 实体缺少此字段 |

**修复方案：**
1. `expiry_date` 已通过 `V1.2__add_expiry_date_to_delivery_detail.sql` 添加
2. 建议在 `DeliveryDetail.java` 添加 `private Integer boxCount;`

---

## 二、delivery_notice（ASN送货通知单表）

| 状态 | 字段 | 说明 |
|------|------|------|
| ✅ | noticeNo → notice_no | 匹配 |
| ✅ | orderId → order_id | 匹配 |
| ✅ | orderNo → order_no | 匹配 |
| ✅ | supplierId → supplier_id | 匹配 |
| ✅ | supplierName → supplier_name | 匹配 |
| ✅ | planDeliveryDate → plan_delivery_date | 匹配 |
| ✅ | actualDeliveryDate → actual_delivery_date | 匹配 |
| ✅ | deliveryStatus | 匹配 |
| ✅ | deliveryMethod | 匹配 |
| ✅ | deliveryCompany | 匹配 |
| ✅ | deliveryNo → delivery_no | 匹配 |
| ✅ | driverName → driver_name | 匹配 |
| ✅ | driverPhone → driver_phone | 匹配 |
| ✅ | vehicleNo → vehicle_no | 匹配 |
| ✅ | deliveryAddress → delivery_address | 匹配 |
| ✅ | receiver | 匹配 |
| ✅ | receiverPhone → receiver_phone | 匹配 |
| ✅ | sendTime → send_time | 匹配 |
| ✅ | arriveTime → arrive_time | 匹配 |
| ✅ | batchNo → batch_no | 匹配 |
| ✅ | productionDate → production_date | 匹配 |
| ✅ | expiryDate → expiry_date | 匹配 |
| ✅ | remark | 匹配 |
| 🟡 | ~~submitTime~~ ← submit_time | DB 有此列，Java 实体缺少 |
| 🟡 | ~~closeTime~~ ← close_time | DB 有此列，Java 实体缺少 |

**修复方案：** 在 `DeliveryNotice.java` 中添加：
```java
private LocalDateTime submitTime;
private LocalDateTime closeTime;
```

---

## 三、receipt_record（收货记录表）

| 状态 | 字段 | 说明 |
|------|------|------|
| ✅ | deliveryId → delivery_id | 匹配 |
| ✅ | noticeId → notice_id | 匹配 |
| ✅ | supplierId → supplier_id | 匹配 |
| ✅ | materialCode → material_code | 匹配 |
| ✅ | materialName → material_name | 匹配 |
| ✅ | planQty → plan_qty | 匹配 |
| ✅ | receiptQty → receipt_qty | 匹配 |
| ✅ | rejectQty → reject_qty | 匹配 |
| ✅ | receiptTime → receipt_time | 匹配 |
| ✅ | receiver | 匹配 |
| ✅ | receiverName → receiver_name | 匹配 |
| ✅ | warehouseId → warehouse_id | 匹配 |
| ✅ | warehouseName → warehouse_name | 匹配 |
| ✅ | location | 匹配 |
| ✅ | receiptStatus | 匹配 |
| ✅ | rejectReason → reject_reason | 匹配 |
| ✅ | remark | 匹配 |
| 🟡 | ~~receiptNo~~ ← receipt_no | DB 有此列（收货单号），Java 实体缺少 |
| 🟡 | ~~diffReason~~ ← diff_reason | DB 有此列（差异原因），Java 实体缺少 |

**修复方案：** 在 `ReceiptRecord.java` 中添加：
```java
private String receiptNo;
private String diffReason;
```

---

## 四、forecast_demand（需求预测表）

| 状态 | 说明 |
|------|------|
| ✅ | **完全匹配，无问题** |

---

## 五、vmi_inventory（VMI库存表）

| 状态 | 说明 |
|------|------|
| ✅ | **完全匹配，无问题** |

---

## 六、delivery_label（送货标签表）

| 状态 | 说明 |
|------|------|
| 🟡 | DB 有 `delivery_label` 表，但**未找到对应的 Java Entity 类** |

> 如果前端/API 不需要操作此表，可以忽略。否则需要创建 `DeliveryLabel.java` 实体。

---

## 七、delivery_plan（交付计划表）

| 状态 | 说明 |
|------|------|
| 🟡 | DB 有 `delivery_plan` 表，但**未找到对应的 Java Entity 类** |

> 同上，按需创建。

---

## 八、⚠️ 有 Entity 但数据库没有对应表（不会自动建表运行时必报错）

| Entity 类 | @TableName | 数据库表 |
|-----------|------------|----------|
| `DeliveryBarcode.java` | `delivery_barcode` | ❌ 不存在 |
| `DeliveryTemplate.java` | `delivery_template` | ❌ 不存在 |
| `DeliveryPackage.java` | `delivery_package` | ❌ 不存在 |
| `DeliveryPackageDetail.java` | `delivery_package_detail` | ❌ 不存在 |
| `ReceiptDiff.java` | `receipt_diff` | ❌ 不存在 |

**修复方案：** 以下二选一——
- **方案A**：如果业务需要这些表，在 `supplier_collaboration.sql` 和 Flyway 中创建对应的建表语句
- **方案B**：如果暂时不需要，删除或用 `@TableName(excludeProperty = ...)` 等方式避免代码扫描到（或直接删除 Entity 类）

---

## 本部分汇总

| 类型 | 数量 | 严重程度 |
|------|------|----------|
| 🔴 CRITICAL（Entity 有字段 DB 无列 → 必报错） | 1（已在 V1.2 修复） | 高 |
| 🟡 字段缺失（DB 有 Entity 无） | 6 | 中 |
| ⚠️ Entity 无对应 DB 表 | 5 | 高（如果被调用必报错） |
| ⚠️ DB 表无对应 Entity | 2 | 低 |