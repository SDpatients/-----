# 5月25日 BUG 汇总 & 待开发功能清单

> **项目：** 供应商协同系统（前端）  
> **技术栈：** Vue 3 + TypeScript + Vite + Element Plus + Pinia  
> **文档日期：** 2026-05-25  
> **文档性质：** 全面审计报告，覆盖所有已发现的 BUG、未完善功能、代码质量问题

---

## 版本说明

本文档是 5月23日汇总的**全面更新版本**。自 5月23日以来，以下问题已修复：

| BUG编号 | 描述 | 状态 |
|---------|------|------|
| BUG-1 | `/orders` 直接访问显示 404 | **已修复** — [`router/index.ts`](file:///d:/.我的项目/供应商协同/frontend/src/router/index.ts#L40-L42) 添加了 `/orders → /purchasing/orders` 重定向 |
| BUG-2 | 供应商审核按钮报"审核状态不能为空" | **已修复** — [`supplier.ts`](file:///d:/.我的项目/供应商协同/frontend/src/api/supplier.ts#L15-L16) 请求体改为 `{ auditStatus: number, auditOpinion?: string }` |
| BUG-3 | Mock 数据状态类型与筛选器不匹配 | **已修复** — [`mockData.ts`](file:///d:/.我的项目/供应商协同/frontend/src/api/mockData.ts#L27-L32) 供应商 status 改为数字 |
| BUG-4 | 供应商资料编辑误用 create 接口 | **已修复** — [`SupplierProfileView.vue`](file:///d:/.我的项目/供应商协同/frontend/src/views/supplier/SupplierProfileView.vue#L61) 改为 `supplierApi.update()` |
| BUG-5 | 报价对比页供应商名称列 v-if="false" | **已修复** — [`QuoteCompareView.vue`](file:///d:/.我的项目/供应商协同/frontend/src/views/purchasing/QuoteCompareView.vue#L87) 移除 `v-if="false"` |
| BUG-6 | 发票作废接口路径 `/cancel` → `/void` | **已修复** — [`finance.ts`](file:///d:/.我的项目/供应商协同/frontend/src/api/finance.ts#L20) |
| BUG-7 | 绩效 API 路径 `/v1/performances` → `/v1/supplier-performances` | **已修复** — [`finance.ts`](file:///d:/.我的项目/供应商协同/frontend/src/api/finance.ts#L55-L64) |
| BUG-8 | 8D 基础路径 `/v1/eight-d` → `/v1/eight-d-reports` | **已修复** — [`qualityExtra.ts`](file:///d:/.我的项目/供应商协同/frontend/src/api/qualityExtra.ts#L26) |
| BUG-9 | 8D 审核路径名 `review` → `audit` | **已修复** — [`qualityExtra.ts`](file:///d:/.我的项目/供应商协同/frontend/src/api/qualityExtra.ts#L36) |
| BUG-10 | 8D 退回路径名 `return` → `reject` | **已修复** — [`qualityExtra.ts`](file:///d:/.我的项目/供应商协同/frontend/src/api/qualityExtra.ts#L38) |
| BUG-11 | NCR 路径 `/v1/ncr` → `/v1/nonconformance-reports` | **已修复** — [`qualityExtra.ts`](file:///d:/.我的项目/供应商协同/frontend/src/api/qualityExtra.ts#L7) |
| BUG-12 | 报价采纳路径名 `adopt` → `accept` | **已修复** — [`sourcing.ts`](file:///d:/.我的项目/供应商协同/frontend/src/api/sourcing.ts#L52) |
| RFQ 详情页 | 点击详情提示"开发中" | **已修复** — 新建 [`RfqDetailView.vue`](file:///d:/.我的项目/供应商协同/frontend/src/views/purchasing/RfqDetailView.vue) |
| Mock 体系 | 未在 main.ts 注册 | **已修复** — [`main.ts`](file:///d:/.我的项目/供应商协同/frontend/src/main.ts#L9-L12) 引入 `setupMock` |

---

## 一、新发现的 BUG（5月25日）

### BUG-N1: 全局搜索组件硬编码 Mock 数据 — 生产环境不可用

| 属性 | 说明 |
|------|------|
| **严重程度** | **中** |
| **影响范围** | 全局搜索功能在所有非 mock 环境下不可用 |
| **文件** | [`GlobalSearch.vue`](file:///d:/.我的项目/供应商协同/frontend/src/components/business/GlobalSearch.vue#L6) |

**根因分析：**

```typescript
// GlobalSearch.vue L6
import { suppliers, orders, asnNotices, qualityCases, settlements } from '@/api/mockData'
```

`GlobalSearch` 组件在 `onClickOutside`、`results` 计算属性中**直接引用本地 mockData**作为搜索数据源。当后端启用时，`mockData` 含有 4 条硬编码供应商、4 条订单等，实际数据库中有成百上千条记录，搜索完全失效。

**影响：**
- 采购方和供应商两侧的布局 header 中都引用了 `<GlobalSearch />`（见 [`PurchasingLayout.vue`](file:///d:/.我的项目/供应商协同/frontend/src/layouts/PurchasingLayout.vue#L61)）
- 用户输入搜索关键词后将只能搜索到 4 条假数据，其他真实记录完全不可见

**建议修复：** 将 `GlobalSearch` 改为调用后端统一的全文检索接口（如 `GET /v1/search?keyword=xxx`），按模块聚合返回结果。

---

### BUG-N2: 供应商端订单中心未传 supplierId 筛选参数

| 属性 | 说明 |
|------|------|
| **严重程度** | **高** |
| **影响范围** | 供应商登录后看到的订单列表 |
| **文件** | [`SupplierOrderCenterView.vue`](file:///d:/.我的项目/供应商协同/frontend/src/views/supplier/SupplierOrderCenterView.vue#L21) |

**根因分析：**

```typescript
// SupplierOrderCenterView.vue L20-22
const result = await orderApi.page(params as any)
```

供应商端 `/supplier/orders` 调用 `orderApi.page()` 时**没有传入 `supplierId` 参数**，后端将返回全部采购订单而非仅本供应商的订单。

对比：
- `SupplierSettlementCenterView.vue` 正确获取了 `supplierId` 并通过 `computed` 计算（见 [L13-L18](file:///d:/.我的项目/供应商协同/frontend/src/views/supplier/SupplierSettlementCenterView.vue#L13-L18)），但该 supplierId 仅用于展示提示，**实际请求也未作为参数传入** API 调用

**建议修复：** 在供应商端所有列表请求中统一传入 `supplierId` 参数，或在后端通过 token 中的供应商身份自动过滤。

---

### BUG-N3: 多处表格列显示 `supplierId` 数字而非供应商名称

| 属性 | 说明 |
|------|------|
| **严重程度** | 中 |
| **影响范围** | 财务中心、VMI 等多页面的数据可读性 |

以下位置表格直接显示供应商 ID 数字，用户无法辨识：

| 文件 | 行号 | 列定义 |
|------|------|--------|
| [`PurchasingFinanceView.vue`](file:///d:/.我的项目/供应商协同/frontend/src/views/purchasing/PurchasingFinanceView.vue#L473) | 473 | 扣款列表 `prop="supplierId"` |
| [`PurchasingFinanceView.vue`](file:///d:/.我的项目/供应商协同/frontend/src/views/purchasing/PurchasingFinanceView.vue#L521) | 521 | 绩效列表 `prop="supplierId"` |
| [`VmiForecastView.vue`](file:///d:/.我的项目/供应商协同/frontend/src/views/purchasing/VmiForecastView.vue#L172) | 172 | VMI 列表 `prop="supplierId"` |
| [`VmiForecastView.vue`](file:///d:/.我的项目/供应商协同/frontend/src/views/purchasing/VmiForecastView.vue#L212) | 212 | 需求预测列表 `prop="supplierId"` |

**根因分析：** 后端可能返回 `supplierId` 字段而非 `supplierName`，但前端未做映射处理。部分创建弹窗中虽使用了 `SupplierSelector` 组件，但表格列仍然直接绑定 `supplierId`。

**建议修复：** 在 API 适配器中添加 `supplierId → supplierName` 的映射，或要求后端在列表接口中关联查询供应商名称并下发。

---

### BUG-N4: NCR 和 8D 创建弹窗未使用 SupplierSelector 组件

| 属性 | 说明 |
|------|------|
| **严重程度** | 中 |
| **影响范围** | 全面质量管理页面的创建体验 |

| 文件 | 行号 | 控件 |
|------|------|------|
| [`PurchasingQualityView.vue`](file:///d:/.我的项目/供应商协同/frontend/src/views/purchasing/PurchasingQualityView.vue#L203) | 203 | NCR 创建：`<el-input v-model="ncrForm.supplierId">` |
| [`PurchasingQualityView.vue`](file:///d:/.我的项目/供应商协同/frontend/src/views/purchasing/PurchasingQualityView.vue#L288) | 288 | 8D 创建：`<el-input v-model="d8Form.supplierId">` |

与 5月23日汇总的第 2.5 节一致 —— 这些弹窗仍未替换为 `SupplierSelector`。

---

### BUG-N5: 消息通知中心分页逻辑缺陷

| 属性 | 说明 |
|------|------|
| **严重程度** | 低 |
| **影响范围** | `/messages` 消息中心的分页和筛选 |
| **文件** | [`MessageCenterView.vue`](file:///d:/.我的项目/供应商协同/frontend/src/views/MessageCenterView.vue#L26-L41) |

**具体问题：**

1. `total` 计算有 Bug（L26）：`total.value = msgData.length >= pageSize.value ? msgData.length + pageSize.value : msgData.length`，当后端返回满页数据时错误地加了一个 pageSize，导致分页器显示多余页数。

2. `loadData()` 中（L111-118）`Promise.all` 内嵌套调用 `loadMessages()` 导致两次加载消息列表，且第一次 `loadMessages` 的结果被忽略。

3. `filteredMessages` 和 `pagedMessages` 做前端内存分页，与后端分页（`loadMessages` 已做了后端分页）产生冲突，`el-pagination` 的 `:total` 绑定 `filteredMessages.length`，分页器行为异常。

---

### BUG-N6: 登录页 `userType` 字段未实际使用

| 属性 | 说明 |
|------|------|
| **严重程度** | 低 |
| **文件** | [`LoginView.vue`](file:///d:/.我的项目/供应商协同/frontend/src/views/LoginView.vue#L10) |

```typescript
const form = reactive({ username: 'admin', password: '123456', userType: 'internal' })
```

`userType` 字段在表单中定义但从未传递给登录 API（`authApi.login({ username, password })`），登录后的路由跳转由 `userStore.user?.userType` 决定，前端选择 userType 的方式未起作用。

---

### BUG-N7: 供应商质量中心 NCR 处理逻辑粗糙

| 属性 | 说明 |
|------|------|
| **严重程度** | 中 |
| **影响范围** | 供应商端质量中心的 NCR 操作 |
| **文件** | [`SupplierQualityCenterView.vue`](file:///d:/.我的项目/供应商协同/frontend/src/views/supplier/SupplierQualityCenterView.vue#L71-L79) |

```typescript
const handleNcr = async (row: NcrRecord) => {
  const nextStatus = row.ncrStatus === 1 ? 2 : 3
  // 无论什么操作，都调用 ncrApi.handle()
  await ncrApi.handle(row.id, { ncrStatus: nextStatus })
}
```

NCR 生命周期有多个操作（提交 → 处理 → 验证 → 关闭），对应不同的后端端点（`submit` / `handle` / `verify` / `close`），此处将两个不同状态的操作合并为一个 `handle` 调用，且前端自己推算 `nextStatus`，绕过后端状态机校验。应分别调用正确的 API 方法。

---

### BUG-N8: 采购发票作废弹窗收集了 `voidReason` 但未传递给 API

| 属性 | 说明 |
|------|------|
| **严重程度** | 低 |
| **影响范围** | 采购方财务中心发票作废功能 |
| **文件** | [`PurchasingFinanceView.vue`](file:///d:/.我的项目/供应商协同/frontend/src/views/purchasing/PurchasingFinanceView.vue#L110) |

```typescript
await invoiceApi.cancel(voidForm.id) // 未传 voidForm.voidReason
```

用户输入了作废原因但未被发送到后端，后端将收到一个没有原因的作废请求，可能影响审计追溯。

---

### BUG-N9: mockInterceptor `paginate` 的状态筛选字符串类型兼容

| 属性 | 说明 |
|------|------|
| **严重程度** | 低 |
| **影响范围** | Mock 模式下的筛选功能 |
| **文件** | [`mockInterceptor.ts`](file:///d:/.我的项目/供应商协同/frontend/src/api/mockInterceptor.ts#L43) |

```typescript
if (status && status !== 'all') {
  filtered = filtered.filter((item) => item.status === status)
}
```

URL 查询参数解析后 `status` 始终为字符串（如 `"1"`），而 `mockData` 中供应商的 `status` 已修正为数字，导致 `"1" === 1` 为 `false`。虽然实际请求经过 axios 的 params 序列化后传递到拦截器时是字符串类型，但 mockInterceptor 内没有做类型转换。

---

## 二、已确认但仍未完善的功能（5月25日更新）

### 2.1 业务详情页（BusinessDetailView）明细 Tab 全为 Mock 数据

| 文件 | [`BusinessDetailView.vue`](file:///d:/.我的项目/供应商协同/frontend/src/views/purchasing/BusinessDetailView.vue#L105-L122) |
|------|------|

`loadDetailData()` 全部通过 `mockApi` 获取数据：
```typescript
orderDetailLines.value = await mockApi.getOrderDetails()
deliveryDetailLines.value = await mockApi.getDeliveryDetails()
inspectionDetailLines.value = await mockApi.getInspectionDetails()
reconDetailLines.value = await mockApi.getReconDetails()
supplierCertificates.value = await mockApi.getCertificates()
```

这 5 个方法在 [`mockApi.ts`](file:///d:/.我的项目/供应商协同/frontend/src/api/mockApi.ts#L64-L67) 中全部返回 `mockData` 的硬编码数据。明细 Tab 之所以能展示数据，完全依赖 mockData 中的假数据。**一旦切换到真实后端环境，业务明细 Tab 将显示空白。** 需要对接后端明细接口或在 mockApi 中改调用真实 API。

---

### 2.2 采购方财务中心 & 供应商财务中心大量代码重复

| 文件 | 对比 |
|------|------|
| [`PurchasingFinanceView.vue`](file:///d:/.我的项目/供应商协同/frontend/src/views/purchasing/PurchasingFinanceView.vue) | 发票/付款/扣款/绩效 四个 Tab |
| [`SupplierSettlementCenterView.vue`](file:///d:/.我的项目/供应商协同/frontend/src/views/supplier/SupplierSettlementCenterView.vue) | 对账/发票/扣款/付款 四个 Tab |

两个组件合计约 **1100 行代码**，其中发票、扣款、付款的 CRUD 逻辑高度重复：

| 功能 | PurchasingFinanceView | SupplierSettlementCenterView | 差异 |
|------|----------------------|------------------------------|------|
| 发票列表 | 含验真、认证、作废操作 | 仅有上传开票 | 采购方操作更多 |
| 扣款列表 | 含提交、确认、入账 | 仅有确认、异议 | 采购方操作更多 |
| 付款列表 | 含创建付款、付款、拒绝 | 只读查看 | 供应商端只读 |
| 对账管理 | 无（在SettlementListView） | 有（确认对账） | 不同模块 |
| 绩效评估 | 有 | 无 | 仅采购方 |

建议抽取公共组件（如 `InvoiceTable`、`PaymentTable`、`DeductionTable`），通过 props 控制权限差异。

---

### 2.3 导出功能未在列表页集成

项目在 [`ExportDialog.vue`](file:///d:/.我的项目/供应商协同/frontend/src/components/business/ExportDialog.vue) 中定义了导出对话框，`OrderListView.vue` 和 `SupplierListView.vue` 已引入该组件。但以下页面仍缺少导出按钮：

| 页面 | 文件 |
|------|------|
| ASN 列表 | [`AsnListView.vue`](file:///d:/.我的项目/供应商协同/frontend/src/views/purchasing/AsnListView.vue) |
| 质量检验列表 | [`QualityListView.vue`](file:///d:/.我的项目/供应商协同/frontend/src/views/purchasing/QualityListView.vue) |
| 对账列表 | [`SettlementListView.vue`](file:///d:/.我的项目/供应商协同/frontend/src/views/purchasing/SettlementListView.vue) |
| 采购方质量中心（NCR/8D/申诉） | [`PurchasingQualityView.vue`](file:///d:/.我的项目/供应商协同/frontend/src/views/purchasing/PurchasingQualityView.vue) |
| 供应商端所有列表 | `SupplierOrderCenterView`/`SupplierDeliveryCenterView`/`SupplierQualityCenterView`/`SupplierSettlementCenterView` |

---

### 2.4 导入导出任务面板未对接后端

`ImportExportPanel.vue` 在多个详情页的 Tab 中被引用，但其内部 `ImportExportTaskList.vue` 仍使用 mock 数据。`importExport.ts` API 模块中的 `templates()` 方法（[line 17-20](file:///d:/.我的项目/供应商协同/frontend/src/api/importExport.ts#L17-L20)）仍返回硬编码假数据。

---

### 2.5 细粒度权限校验已启用但权限覆盖不完整

[`router/index.ts`](file:///d:/.我的项目/供应商协同/frontend/src/router/index.ts#L123-L126) 的权限检查已启用：
```typescript
if (requiredPermission && !userStore.permissions.includes(requiredPermission)) {
  return '/403'
}
```

但以下路由缺少 `permission` meta：

| 路由路径 | 缺失的 permission |
|----------|-------------------|
| `/purchasing/quotes` | 报价对比页无权限限制 |
| `/purchasing/vmi` | VMI 预测页无权限限制 |
| `/purchasing/integration` | 集成网关页无权限限制 |
| `/purchasing/settings` | 系统配置页无权限限制 |
| `/purchasing/finance` | 财务中心无权限限制 |
| `/purchasing/quality-ext` | 全面质量管理无权限限制 |
| 供应商端 `/supplier/*` | 所有子路由均已补全权限 |

---

### 2.6 消息通知中心 -- 已部分完善

[`MessageCenterView.vue`](file:///d:\.我的项目\供应商协同\frontend\src\views\MessageCenterView.vue) 已实现：
- 消息列表加载
- 未读/已读/全部筛选
- 全部已读/批量已读
- 待办列表展示
- 消息等级标签

但仍存在的问题见 **BUG-N5**。

---

### 2.7 供应商资质证书管理 -- 已开发但使用 Mock 数据

[`SupplierProfileView.vue`](file:///d:/.我的项目/供应商协同/frontend/src/views/supplier/SupplierProfileView.vue) 已完整实现资质证书的 CRUD（增删改查）、到期提醒、状态计算，并调用了 `qualificationApi`。但 API 模块 [`qualification.ts`](file:///d:/.我的项目/供应商协同/frontend/src/api/qualification.ts) 尚未与后端对接验证。

---

### 2.8 分页器缺少空状态保护

多个页面的 `el-pagination` 组件在数据为空时，`total` 为 0 但 `pageNum` 不变，可能导致用户点击"第 1 页"时发起请求但数据为空。此外，在某些页面中 `@current-change` 和 `@size-change` 只调用了 `loadData`，未处理 loading 状态下的重复请求。

---

## 三、接口与后端对接状态（审计更新）

### 3.1 已对接验证通过的 API 模块

| 模块 | 文件 | 验证结果 |
|------|------|----------|
| 供应商 | [`supplier.ts`](file:///d:/.我的项目/供应商协同/frontend/src/api/supplier.ts) | **通过** — page/detail/create/update/audit 参数与后端匹配 |
| 订单 | [`order.ts`](file:///d:/.我的项目/供应商协同/frontend/src/api/order.ts) | **通过** |
| 物流/ASN | [`logistics.ts`](file:///d:/.我的项目/供应商协同/frontend/src/api/logistics.ts) | **通过** |
| 质量检验 | [`quality.ts`](file:///d:/.我的项目/供应商协同/frontend/src/api/quality.ts) | **通过** |
| 对账 | [`settlement.ts`](file:///d:/.我的项目/供应商协同/frontend/src/api/settlement.ts) | **通过** |
| 库存/VMI | [`inventory.ts`](file:///d:/.我的项目/供应商协同/frontend/src/api/inventory.ts) | **通过** |
| 集成网关 | [`integration.ts`](file:///d:/.我的项目/供应商协同/frontend/src/api/integration.ts) | **通过** |
| 系统配置 | [`config.ts`](file:///d:/.我的项目/供应商协同/frontend/src/api/config.ts) | **通过** |
| 附件 | [`attachment.ts`](file:///d:/.我的项目/供应商协同/frontend/src/api/attachment.ts) | **通过** |
| 操作日志 | [`operationLog.ts`](file:///d:/.我的项目/供应商协同/frontend/src/api/operationLog.ts) | **通过** |
| 财务 | [`finance.ts`](file:///d:/.我的项目/供应商协同/frontend/src/api/finance.ts) | **通过** — 已修复所有路径错误 |
| 质量管理 | [`qualityExtra.ts`](file:///d:/.我的项目/供应商协同/frontend/src/api/qualityExtra.ts) | **通过** — 已修复所有路径错误 |
| 寻源/报价 | [`sourcing.ts`](file:///d:/.我的项目/供应商协同/frontend/src/api/sourcing.ts) | **通过** — 已修复 accept 路径 |
| 消息通知 | [`notification.ts`](file:///d:/.我的项目/供应商协同/frontend/src/api/notification.ts) | **通过** |
| 资质证书 | [`qualification.ts`](file:///d:/.我的项目/供应商协同/frontend/src/api/qualification.ts) | **待验证** — 尚未与后端联调 |

### 3.2 Mock 数据体系状态

| 项目 | 状态 |
|------|------|
| `mockInterceptor.ts` 注册 | **已注册** — `main.ts` 中通过 `setupMock(request)` 启用 |
| Mock 路由覆盖 | **部分覆盖** — 覆盖了 Auth/Dashboard/Supplier/Order/Delivery/Quality/Recon/Timeline/Todo/Attachment/Export/Import/AuditLog/Message/Common/Config/VMI/Forecast/Finance |
| **Mock 未覆盖的模块** | **NCR/8D/Appeal/Quote/RFQ** 在 mockInterceptor 中没有对应处理器，Mock 模式下这些页面将显示空数据或报错 |

---

## 四、代码质量问题

### 4.1 行内样式与 scoped style 混用

多个页面中同时使用 `class="mt-4"` 和 `<style scoped>` 中定义的 `.mt-4` 类，但 `mt-4` 并非全局工具类，不同文件各自定义，维护困难。建议引入 Tailwind CSS 或统一全局工具类。

### 4.2 错误处理不一致

所有 API 调用使用 `try { ... } catch { /* 拦截器处理 */ }` 模式，但没有统一的错误展示策略：
- 部分 catch 块完全为空
- 部分 catch 块有 `if (e !== 'cancel' && e !== 'close')` 的手动判断
- 部分使用 `ElMessage.error()` 显示错误

### 4.3 any 类型滥用

大量代码使用 `any` 类型：
- `const res = await xxxApi.page(params as any)` 模式出现在几乎所有列表页
- 表单 `reactive` 对象使用 `as any` 传递给 API

建议完善 `types/business.ts` 中的类型定义，减少 `any` 使用。

### 4.4 未使用的导入

以下文件存在未使用的导入：

| 文件 | 未使用的导入 |
|------|-------------|
| [`SupplierProfileView.vue`](file:///d:/.我的项目/供应商协同/frontend/src/views/supplier/SupplierProfileView.vue#L3) | `watch` 已导入但从未使用 |

### 4.5 operationLog.ts 死代码

[`operationLog.ts`](file:///d:/.我的项目/供应商协同/frontend/src/api/operationLog.ts#L5-L9) 顶部定义的 `logs` 数组（3 条硬编码操作日志）从未被使用，实际 API 调用直接走后端。

---

## 五、总结

### 统计总表

| 类别 | 数量 | 关键项 |
|------|------|--------|
| 5月23日已修复 | **13** | 路由/审核/Mock/API路径等 |
| 新发现 BUG | **9** | BUG-N1 ~ BUG-N9 |
| 功能未完善 | **8** | 明细对接、代码重复、导出集成等 |
| 代码质量问题 | **5** | 类型滥用、错误处理、死代码等 |

### 优先建议

1. **紧急（P0）：** 修复 BUG-N2（供应商端订单数据泄漏）— 供应商可能看到其他供应商的订单
2. **高优（P1）：** 修复 BUG-N1（全局搜索仅搜索 mock 假数据）— 生产环境搜索完全失效
3. **高优（P1）：** 补充 mockInterceptor 中 NCR/8D/Appeal/Quote/RFQ 的 Mock 处理器
4. **高优（P1）：** 将 BusinessDetailView 的明细数据源从 mockApi 切换为真实 API
5. **中优（P2）：** 修复 BUG-N3（表格 supplierId → supplierName 显示）
6. **中优（P2）：** 修复 BUG-N4（NCR/8D 弹窗使用 SupplierSelector）
7. **中优（P2）：** 修复 BUG-N5（消息中心分页逻辑）
8. **中优（P2）：** 补全所有页面的导出功能按钮
9. **中优（P2）：** 补全路由权限 meta
10. **低优（P3）：** 修复 BUG-N6/N7/N8/N9 及其他代码质量问题
11. **低优（P3）：** 抽取公共财务/结算表格组件，消除 PurchasingFinanceView & SupplierSettlementCenterView 重复代码